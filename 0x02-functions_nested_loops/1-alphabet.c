# include "main.h"
# include <stdio.h>
/**
* main - Short description, single line
* Return: Description of the returned value
*/
void print_alphabet(void);
int main(void)
{
char *c[300];

void print_alphabet(void);
{
int i=0;

for (i = 97; i < 123; i++)
c[i] = putchar (i);
return(c);
}
putchar (10);
}
