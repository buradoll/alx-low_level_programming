#include <stdio.h>
/**
* main - Short description, single line
* Return: Description of the returned value
*/
int main(void)
{
int i = 0;

for (i = 48; i <= 57; i++)
putchar(i);
for (i = 65; i <= 70; i++)
putchar (i);
putchar (10);
return(0);
}
