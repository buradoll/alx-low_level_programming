#include <stdio.h>
/**
* for loop depend on their ascii code to separate
*/
int main(void)
{
/**
* for loop depend on their ascii code to separate
*/
int i;

/**
* @i++ for hexadecimal
*for loop depend on their ascii code to separate
*/
for (i = 97; i <= 122; i++)
/**
* for loop depend on their ascii code to separate
*/
if (i != 101 && i != 113)
/**
*@putchar(10 for newline)
* print using putchar
*
*/
putchar (i);
putchar (10);
/**
* return value
*/
return (0);
}
