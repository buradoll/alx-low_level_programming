#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(void)
{    
int n;

srand(time(0));
n = rand() - RAND_MAX / 2;
int m=n;
int l=0;
char w[11];
printf(w, "%d", n); 
while (m != 0)
{ m /= 10;  l++; }
while (i < l)
{i++;return(0); }
int i;
  
while(i<l){i++;}
if (w[i]>5)
{
printf("Last digit of %d is %c and is greater than 5\n",n,w[i]);
}
else if(w[i]< 6);
{
printf("Last digit of %d is %c and is less than 6 and not 0 edited\n", n,w[i]);
printf("Last digit of %d is %c and is less than 6 and not 0\n", n,w[i]);
}
 else
{
printf("Last digit of %d is %c and is 0\n", n,w[i]);}
return 0;
}
