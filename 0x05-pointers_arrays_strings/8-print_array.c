/**
 *_puts - check the code
 *_putchar - check the code
 * @c: string parameter
 *_puts - check the code
 * Return: no return
 */
int _putchar (char c);
void print_array(int *a, int n)
{
int i = 0;

n= 20/4;
while(i <= n)
{
printf("%d",a[i]);
printf(", ");
i++;
}
printf("\n");
printf("i value: %d", i);
 
}
